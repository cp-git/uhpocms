spring:
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/uhpocms
    username: postgres
    password: Pass1word

#server:
#  port: 8090
  
server:
  ssl:
    key-store-password: sslpass
    key-store: src/main/resources/keystore/javadevjournal.p12
    key-store-type: PKCS12
    key-alias: javadevjournal
  port: 8443
  
file:
  base-path: E:/uploadFiles

