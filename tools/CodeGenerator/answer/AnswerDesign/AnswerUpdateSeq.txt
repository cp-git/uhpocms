@startuml
actor user
participant AnswerApplication as uhpoc
participant AnswerController as main
participant CPException as cp
participant ResponseHandler as rh
participant AnswerService as serv
participant AnswerServiceImpl as impl
participant AnswerRepository as repo
database DATABASE as db
activate uhpoc
activate user
  user-->uhpoc : Enter URL-[http://localhost:8090/uhpocms/answer/{id}]\n and Enter Data
    alt#Gold Case: If any Exception occur
    uhpoc-> main : 
    activate main
    main->cp : throws CPException
    activate cp
    cp-> user : Answer Update Failed 
    deactivate cp
    else  Case: If no exception occur
     uhpoc -> main : main()
          main -> serv : updateAnswerById()
          activate serv
          serv -> impl : updateAnswerById()
          activate impl
          impl -> repo : findByAnswerId()
          activate repo
          repo -> db : Fetching Answer by Id
          activate db
           db->db : Fetching data
          db --> repo : Data of Answer 
          deactivate db
          repo --> impl :  Data of Answer
          deactivate repo
          impl -> repo : Setting updated value and save()
          activate repo
          repo->db :  Setting updated value and save
          activate db
          db->db : Store data into db
          db->repo : updated data
          deactivate db
          repo->impl : updated data
          deactivate repo
          impl->serv : updated data
          deactivate impl
          serv --> main :  Updated data 
          deactivate serv
          alt#Gold If update Answer is not null
          main->rh : generateResponse()
          activate rh
          rh->rh : generating response
          rh->main : response with HttpStatus code 201
          deactivate rh
          main --> uhpoc :  Return updated data and return HttpStatus code 201
          uhpoc--> user :  Return updated data and return HttpStatus code 201
          else If update Answer is Null
           main->rh : generateResponse()
          activate rh
          rh->rh : generating response
          rh->main : response with HttpStatus code 500
          deactivate rh
          main->uhpoc : Return Answer updation failed with HttpStatus Code 500
          deactivate main
          uhpoc->user : Return Answer updation failed with HttpStatus Code 500
          deactivate uhpoc
          deactivate user
      end
      end
@enduml
